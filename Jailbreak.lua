local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Vzltex/Senpai-Lib/master/UILibrary"))();

local Window1 = library:CreateWindow("Teleports");
Window1:Button("Bank",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(40, 16.068, 786);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Jewl",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(136.031, 16.976, 1353.04);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Crim1",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(-235, 17.225, 1623);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Crim2",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(1638, 48.136, -1767);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Prison",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(-1218, 38.607, -1575);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("1 Mil",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(351.762, 20, -1700.921);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Radio",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(1034.241, 98.43, 1104.152);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Bridge1",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(598, 60.669, 1032);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Bridge2",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(666, 60.32, 1702);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("museum",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(1075.503, 136.328, 1228.639);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Gun",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(-20, 15.968, -1756);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Server",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(64, 16.066, 1137);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Area 51",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(813, 18.899, -315);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Ski",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(17.537, 18.844, 2372.027);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);
Window1:Button("PowerPlant",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(720, 37.636, 2351);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("JetPack",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(1375.901, 173.199, 2585.686);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);
Window1:Button("AirPort",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(-1592, 40.072, 2859);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Port",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(-393.817657, 23.5516968, 2020.49902);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

Window1:Button("Pirate Ship",function()
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "x0zdpcxj"
}
Client.FireServerT:FireServer(unpack(Arguments))
local plr = game:service"Players".LocalPlayer;
local tween_s = game:service"TweenService";
local info = TweenInfo.new(0.42,Enum.EasingStyle.Quad);
function tp(...)
   local tic_k = tick();
   local params = {...};
   local cframe = CFrame.new(params[1],params[2],params[3]);
   local tween,err = pcall(function()
       local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
       tween:Play();
   end)
   if not tween then return err end
end
tp(1875.797, 40, 1895.154);
wait(0.00)
        local Client = {}

        for i,v in pairs(debug.getregistry()) do
            if typeof(v) == "function" then
                for i2,v2 in pairs(debug.getupvalues(v)) do
                    if typeof(v2) == "table" and v2.FireServer then
                        Client.FireServerT = v2
                    end
                end
            end    
        end    
    

local Arguments = {
        [1] = "l4n0dq65"
}
Client.FireServerT:FireServer(unpack(Arguments))
end);

loadstring(game:HttpGet("https://pastebin.com/raw/x9phkXSp"))();
